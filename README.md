## SidedBlacklist
Blacklist certain mods to be loaded for certain side (client/server).  
Useful for modpacks to blacklist client-side mods that doesn't declare themselves as clientSideOnly on the dedicated server, so the pack can be on server without manually removing those mods.  